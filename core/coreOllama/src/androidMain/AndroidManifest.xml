<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Required for model downloading from Ollama registry -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional: for persistent server after app closes -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <!-- Ollama ForegroundService for running the server subprocess -->
        <service
            android:name=".OllamaForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Broadcast receiver to restart on boot (optional) -->
        <receiver
            android:name=".OllamaBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICK_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
